{% extends 'layout.html.twig' %}

{% block content %}
	<h1>Users</h1>

	{% for user in users %}
		<article>
			<h1>{{ user.firstname }}
				{{ user.lastname }}</h1>
		</article>
	{% endfor %}
{% endblock %}
